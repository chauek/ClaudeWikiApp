## Highlights
- Tasks toolbar now leads with **By priority** (the default view), with **By section** second.
- New Claude-driven release flow with human-reviewed notes baked into each GitHub Release.

## Features
- Reorder Tasks view toggle so "By priority" sits before "By section".
- Add `claude_release.sh` script that refuses to ship without an approved `docs/releases/v<ver>.md` notes file; release workflow now uses that file as the Release body when present.

## Docs & Chore
- New `docs/release-process.md` describing the patch/minor/major flow and post-hoc notes path; CLAUDE.md trimmed to a pointer.
- Architecture doc resynced with current code (IPC handlers, App state, shared types).
- Scaffold CLAUDE.md warns users to put overrides in `CLAUDE.local.md`; scaffold version bumped to 6.
- Backfilled `docs/releases/v0.2.0.md` for the prior tag.
