## Highlights
- Settings no longer suggests downgrading your knowledge base scaffold when it's already newer than the bundled version.

## Fixes
- Scaffold update prompt now only appears when the app's bundled scaffold version is newer than the installed one.
