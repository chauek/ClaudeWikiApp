## Highlights
- Release builds now target x86-64 (Intel/Rosetta) instead of ARM, fixing launch issues on unsigned builds blocked by Gatekeeper.

## Fixes
- Scaffold update prompt no longer suggests downgrading when the installed version is already newer than the bundled one.

## Improvements
- CI runner switched from macos-14 (ARM) to macos-13 (x64) to match target architecture.
